package system.stays;

import application.page_library.HomePage;
import application.page_library.SearchResultsPage;
import base.BasePage;
import org.testng.annotations.Test;

public class TestStays extends BasePage {

//    anti bot tab opens when clicking on hotel link with driver
//    @Test (groups = {"smoke", "stays"}, dataProviderClass = data_providers.DataProviders.class, dataProvider = "testViewStay")
//    public void testViewStay(String destination, String index, String startDate, String endDate, String stayOption) {
//
//        HomePage homePage = new HomePage();
//
//        SearchResultsPage searchResultsPage = homePage.searchForStay(destination, Integer.parseInt(index),
//                Integer.parseInt(startDate), Integer.parseInt(endDate));
//
//        searchResultsPage.selectStay(Integer.parseInt(stayOption));
//
//    }

}
